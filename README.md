# ADMIN-DXMOD-CHONG-CRACK (Standalone Core Payload Server)

Server độc lập phân phối mã nguồn lõi mã hóa động (Server-Driven Code Injection) cho DX MOD. Chống crack 100% bằng cách chỉ nạp tính năng lõi vào RAM khi UID được cấp phép VIP.

## Cấu Hình & Chạy Server

```bash
cd CORE-SERVER
npm install
npm start
```

Default Port: `5001`
